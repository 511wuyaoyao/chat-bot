# data_engine

数据工具的底层文件引擎目录，负责把用户可见知识库映射到 `data/{userId}` 下的文件系统结构。

- `file-engine.ts`：提供文件夹、文件、条目的增删改查，以及目录树扫描。
- `entities.ts`：定义条目和目录树相关类型。

边界：本目录只能暴露用户知识库内容；`session/` 等运行时状态目录必须视为内部数据，不得进入 Attention 上下文或工具返回。
